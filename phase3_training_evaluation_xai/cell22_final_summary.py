# ==============================================================
# CELL 22 — Final Summary (PUBLICATION-READY ALIGNMENT)
# Strictly mirrors active execution logic to pass rigorous peer review
# ==============================================================
print("\n" + "="*65)
print("  FINAL EXPERIMENT SUMMARY — Swin-T Tea Leaf Classifier")
print("="*65)
print(f"  Classes    : {CLASS_NAMES}")
print(f"  Dataset    : TeaLeafNet  (5,000 images, 4 classes)")
print(f"  Split      : 70% Train / 15% Val / 15% Test (stratified)")
print(f"  Backbone   : Swin-Tiny (pretrained ImageNet-1k via timm)")
print(f"  Head       : GAP → FC(512,ReLU) → Dropout → FC(4)")
print(f"  Phase 1    : Frozen backbone  | AdamW | LR=1e-4 | 8 ep  | Drop=0.30 | MixUp α=0.20")
print(f"  Phase 2    : Stage-4 unfrozen | SGD   | LR=5e-5 | 15 ep | Drop=0.20 | MixUp α=0.15")
print(f"  Regularize : Dropout + MixUp + LabelSmooth=0.08 + GradClip=1.0 + StochDepth=0.2")
print(f"  Scheduler  : Linear Warmup + Cosine Annealing")
print(f"  Augment    : Crop+Flip+Rotate+Blur+Erase+MixUp (Simulated Environmental Variance)")
print(f"  Evaluation : TTA ×{N_TTA} (Test Time Augmentation via Spatial/Photometric Shifts)")
print(f"  DataLoader : num_workers=0 | pin_memory cached in RAM")
print("─"*65)
print(f"  Test Accuracy (TTA×{N_TTA}) : {acc*100:.2f}%")
print(f"  Precision (Weighted)  : {prec:.4f}")
print(f"  Recall    (Weighted)  : {rec:.4f}")
print(f"  F1-Score  (Weighted)  : {f1:.4f}")
print(f"  Latency               : {lat_mean:.2f} ± {lat_std:.2f} ms  ({fps:.1f} FPS)  [{DEVICE}]")
print("─"*65)
print("  Figures saved  : fig1 … fig9  (.png)")
print("  Checkpoint     : swin_tea_best.pth")
print("="*65)